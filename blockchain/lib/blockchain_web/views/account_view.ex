defmodule BlockchainWeb.AccountView do
  use BlockchainWeb, :view

  # def render("index.json", %{users: users}) do
  #   %{data: render_many(users, BlockchainWeb.AccountView, "user.json")}
  # end

  # def render("user.json", %{user: user}) do
  #   %{name: user.name, password_hash: user.password_hash}
  # end

  # def render("create.json", %{user: user}) do
  #   "User #{user.name} created!"
  # end

  # def render("delete.json", %{user: user}) do
  #   "User #{user.name} deleted!"
  # end
end
